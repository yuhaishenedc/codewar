package leetcode;

public class ListNode_234 {
    /*
class Solution {
public:
    bool isPalindrome(ListNode* head) {
        stack<ListNode*> s;
        ListNode* tmp=head;
        while(tmp!=NULL){
            s.push(tmp);
            tmp=tmp->next;
        }
        while(head!=NULL){
            ListNode* cmp=s.top();
            s.pop();
            if(cmp->val!=head->val){
                return false;
            }
            head=head->next;
        }
        return true;
    }
};
     */
}

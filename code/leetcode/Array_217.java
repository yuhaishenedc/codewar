package leetcode;

public class Array_217 {
    /*
class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {
        set<int> s;
        int sc=nums.size();
        for(int i=0;i<sc;i++){
            if(s.find(nums[i])!=s.end()){
                return true;
            }
            else{
                s.insert(nums[i]);
            }
        }
        return false;
    }
};
     */
}

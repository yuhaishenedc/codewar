package leetcode;

public class String_125 {
    /*
class Solution {
public:
    bool isPalindrome(string s) {
        vector<char> v;
        string tmp=s;
        int sc=s.size();
        for(int i=0;i<sc;i++){
            char x=tolower(s[i]);
            if(x-'0'>=0&&x-'0'<=9||x-'a'>=0&&x-'a'<=25){
                v.push_back(x);
            }
        }
        vector<char> com(v);
        reverse(v.begin(),v.end());
        int l=v.size();
        for(int i=0;i<l;i++){
            if(v[i]!=com[i]){
                return false;
            }
        }
        return true;
    }
};
     */
}

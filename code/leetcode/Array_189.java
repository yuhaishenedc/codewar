package leetcode;

public class Array_189 {
    /*
class Solution {
public:
    void rotate(vector<int>& nums, int k) {
        int sc=nums.size();
        int count=0;
        for(int i=0;count<sc;i++){
            int left=nums[i];
            int cur=i;
            do{
                int next=(cur+k)%sc;
                int temp=nums[next];
                nums[next]=left;
                left=temp;
                cur=next;
                count++;
            }while(cur!=i);
        }
    }
};
     */
}
